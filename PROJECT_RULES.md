# プロジェクト運用ルール

最終更新: 2026-05-14

## 1. フォルダ管理ルール

- `petlifeplus-site/` は、本プロジェクトで生成される成果物を配置する専用フォルダとする。
- `petlifeplus-site/` は、プロジェクトアーカイブ本体（`index.html`, `docs/`, `design/`, `prompts/`, `app/` など）とは分離して管理する。
- プロジェクトアーカイブ本体の修正作業では、`petlifeplus-site/` を対象外とする。

## 2. 変更時の判断ルール

- 変更依頼が「プロジェクトアーカイブ」の体裁・構成・文書整備に関する場合は、`petlifeplus-site/` を編集しない。
- `petlifeplus-site/` を変更するのは、成果物サイト自体の修正依頼が明示された場合のみとする。

## 3. スタイル変更ルール

- スタイル変更は、ページ内 `<style>` やインライン指定よりも、共通スタイルシート（`css/style.css`）での変更を優先する。
- 個別ページのスタイル調整が必要な場合も、まずは共通クラス追加で対応し、ページ専用CSSは最小限に抑える。
