services:
  - type: web
    name: portfolio
    env: python
    plan: free
    buildCommand: ""
    startCommand: python3 -m http.server $PORT --bind 0.0.0.0
    healthCheckPath: /
    autoDeploy: true
